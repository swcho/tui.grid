import { SelectionRange, Range } from '../store/types';
export declare function isSameInputRange(inp1: SelectionRange | null, inp2: SelectionRange | null): boolean;
export declare function getSortedRange(range: Range): Range;
